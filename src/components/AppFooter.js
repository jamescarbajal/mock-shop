import { FooterContainer } from './StyledComponents'

export default function AppFooter() {

    return (
        <FooterContainer>
            <div class="site-footer">
                This is the footer!
            </div>
        </FooterContainer>
    );
};